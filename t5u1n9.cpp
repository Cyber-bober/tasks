#include <iostream>

using namespace std;

int main() {
    int n;
    double s = 0, a = 1;
    cout << "n: ";
    cin >> n;
    for (int = 1; i <= n; i++)
    {
        a *= i;
        s += a;
    }
    cout << "s = " << s << endl;
    return 0;
}