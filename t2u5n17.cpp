#include <iostream>
using namespace std;
int main()
{
	int n;
	double x, p = 1;
	cout << "x = ";
	cin >> x;
	double b = x;
	cout << "i = ";
	cin >> i;
	for (int i; n <= i; n++)
	{
		b *= -pow(x,2);
		p *= (1+ b / (pow(n, 3) + pow (n, 2)))
	}
	cout << "p = " << p << endl;
	return 0;
}
